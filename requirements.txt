# crewai-tools declares langchain-community<0.4.0,>=0.3.1, a range wide enough
# that pip walks dozens of builds before it settles. Pinning the packages the
# resolver thrashes on gives it a fixed point and the install resolves in one
# pass. If a pin ever conflicts, pip fails immediately with a readable message
# instead of backtracking for twenty minutes.
crewai==0.108.0
crewai-tools==0.33.0

langchain-community==0.3.26
dataclasses-json==0.6.7

python-dotenv>=1.0
fastapi>=0.115
uvicorn[standard]>=0.30
boto3>=1.34

pytest>=8.0
